#import the library turtle
import turtle
skk = turtle.Turtle()
skk.speed(0)
#write a for loop where the range(4)
for i in range(4):
#write skk.forward and pass it with an integer 50
    skk.forward(50)
#write skk.right and pass it with an integer 90
    skk.right(90)
turtle.done()
